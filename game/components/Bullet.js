/**
 * このコンポーネントを持つエンティティが「弾丸」であることを示すタグ。
 */
export class Bullet {
    /**
     * @param {object} [config] - 設定オブジェクト（現在は未使用）
     */
    constructor(config = {}) {
      // 引数なしコンポーネントのため、中身は空
    }
  }
  