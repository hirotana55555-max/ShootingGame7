/**
 * プレイヤーによって操作可能であることを示すマーカーコンポーネント。
 * このコンポーネントが存在するエンティティは、InputSystemによって操作される。
 */
export class Controllable {
  /**
   * @param {object} [config] - 設定オブジェクト（現在は未使用）
   */
  constructor(config: {} = {}) {
    // 引数なしコンポーネントのため、中身は空
  }
}
