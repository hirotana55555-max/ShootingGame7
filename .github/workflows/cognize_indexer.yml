name: Cognize Indexer

on:
  push:
    branches:
      - main
    paths-ignore:
      - 'Project_Cognize/workspace/outputs/**'
      - 'Project_Cognize/database/**'
      - '**.md'
  
  pull_request:
    branches:
      - main
  
  workflow_dispatch:
    inputs:
      mode:
        description: '実行モード (dry-run / production)'
        required: true
        default: 'dry-run'
        type: choice
        options:
          - dry-run
          - production
      full_scan:
        description: 'フルスキャン実行'
        required: false
        type: boolean
        default: false

permissions:
  contents: write
  pull-requests: read

jobs:
  index:
    runs-on: ubuntu-latest
    
    steps:
      - name: リポジトリのチェックアウト
        uses: actions/checkout@v4
        with:
          fetch-depth: 2  # 差分比較のため直前のコミットも取得

      - name: Node.js のセットアップ
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: 依存関係のインストール
        run: npm ci

      - name: データベースディレクトリの作成
        run: mkdir -p Project_Cognize/database

      - name: インデクサー実行（dry-run）
        if: github.event_name == 'pull_request' || github.event.inputs.mode == 'dry-run'
        run: |
          ARGS="--dry-run --verbose"
          if [ "${{ github.event.inputs.full_scan }}" == "true" ]; then
            ARGS="$ARGS --full-scan"
          fi
          node Project_Cognize/scripts/indexer.js $ARGS

      - name: インデクサー実行（本番）
        if: github.event_name == 'push' && github.ref == 'refs/heads/main' && github.event.inputs.mode != 'dry-run'
        run: |
          ARGS="--verbose"
          if [ "${{ github.event.inputs.full_scan }}" == "true" ]; then
            ARGS="$ARGS --full-scan"
          fi
          node Project_Cognize/scripts/indexer.js $ARGS

      - name: JSONL検証
        run: |
          if [ -f "Project_Cognize/workspace/outputs/static_index.jsonl" ]; then
            echo "JSONL検証開始..."
            head -n 100 Project_Cognize/workspace/outputs/static_index.jsonl | jq empty
            echo "✓ JSONL形式正常"
          fi

      - name: 統計情報の表示
        run: |
          if [ -f "Project_Cognize/database/static_index.db" ]; then
            node Project_Cognize/scripts/query_index.js stats
          fi

      - name: 変更の確認
        id: check_changes
        run: |
          if git diff --quiet Project_Cognize/workspace/outputs/ Project_Cognize/database/; then
            echo "changes=false" >> $GITHUB_OUTPUT
          else
            echo "changes=true" >> $GITHUB_OUTPUT
          fi

      - name: 変更のコミット
        if: steps.check_changes.outputs.changes == 'true' && github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add Project_Cognize/workspace/outputs/ Project_Cognize/database/
          git commit -m "chore: update Cognize index [skip ci]" || echo "変更なし"
          git push

      - name: アーティファクトのアップロード（PR用）
        if: github.event_name == 'pull_request'
        uses: actions/upload-artifact@v4
        with:
          name: cognize-index-preview
          path: |
            Project_Cognize/workspace/outputs/static_index.jsonl
            Project_Cognize/database/static_index.db
          retention-days: 7

      - name: PRコメント（統計情報）
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const { execSync } = require('child_process');
            
            try {
              const stats = execSync('node Project_Cognize/scripts/query_index.js stats', {
                encoding: 'utf8'
              });
              
              const comment = `## 📊 Cognize インデックス統計\n\n\`\`\`\n${stats}\n\`\`\``;
              
              github.rest.issues.createComment({
                issue_number: context.issue.number,
                owner: context.repo.owner,
                repo: context.repo.repo,
                body: comment
              });
            } catch (error) {
              console.log('統計情報の投稿をスキップ');
            }
